<% include partials/header.ejs %>
    <div class="container">
        
        <div class="card mt-2 mb-2">
            <div>
                <a class="btn btn-primary" href="/"> < continuar comprando</a>    
            </div>
            <% var prodList = prods.items;%>
            <% if(prods.isEmpty){%>
                <h5>Carrinho vazio</h5>
            <% } else {%>
                
                
                <h5>Produtos:</h5>
                <div class="row mt-2">
	                <%var prod;%>
	                
                    <%for(var x in prodList){%>
	                <%prod = prodList[x];%>
                    
                    <div class="col-md-4 col-sm-6 mb-5">
        	            <div class="card" style="width: 18rem;">
                          <img src="<%=prod.img%>" class="card-img-top" alt="...">
                          <div class="card-body">
                            <h5 class="card-title"> <%=prod.name%> - Quantidade: <%=prod.qtd%> </h5>
                            <p class="card-text"> <%=prod.desc%> </p>
                            <h5 class="card-title" style="color: green">R$  <%=prod.preco%> </h5>
                            <a href="/cart/delete/<%=prod.id%>" class="btn btn-danger btn-sm" >- carrinho</a>
                            <!-- <form method="POST" action="/itemDelete">
                                <input type="hidden" name="prod" value="<%=prod._id%>">
                                <button type="submit" class="btn btn-danger btn-sm" >- carrinho</button>
                            </form> -->
                            
                          </div>
                        </div>
                    </div>
                    <% } %>
	                
	    </div>
               <h5 class="ml-2">Total: R$<%=prods.precoTotal%></h5> 
               <a class="btn btn-success" href="/compra">Finalizar compra</a>
            <% } %>
        </div>
    </div>

 <% include partials/footer.ejs %>
